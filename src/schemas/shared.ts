export type Seo = {
  metaTitle: string,
  metaDescription: string
}

export type SocialURL = {
  socialNetwork: string,
  URL: string
}

export type Theme = {
  bodyBg?: string,
  bodyColor?: string,
  fontFamily?: string
}
